# Raspberry Pi Zero 2 W — Configs

Store here: OS/build configs (e.g. `config.txt`, `cmdline.txt`), image build configs (Buildroot/Yocto), and application configs for services running on the device.
