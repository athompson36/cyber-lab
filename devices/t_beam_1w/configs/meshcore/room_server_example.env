# MeshCore T-Beam 1W — Room Server example build/config
# Copy to firmware repo or use as reference for platformio.ini
# Env: T_Beam_1W_SX1262_room_server

# Device identity
ADVERT_NAME=MyTBeamRoomServer

# Admin and room access (set your own in production)
ADMIN_PASSWORD=changeme
ROOM_PASSWORD=roomchangeme

# Room server may have additional options (e.g. room ID, max clients)
# See MeshCore docs and platformio.ini for this env.
