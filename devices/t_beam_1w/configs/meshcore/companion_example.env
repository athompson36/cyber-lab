# MeshCore T-Beam 1W — Companion (BLE) example build/config
# Copy to firmware repo or use as reference for platformio.ini build_flags / build_type
# Env: T_Beam_1W_SX1262_companion_radio_ble

# Device identity (advertised name)
ADVERT_NAME=MyTBeamCompanion

# Admin / security (set your own in production)
ADMIN_PASSWORD=changeme

# Region (868 or 915; match your hardware)
# BUILD_FLAGS often set in platformio.ini per env

# Optional: room server URL if companion talks to a room server
# ROOM_SERVER_URL=
