# MeshCore T-Beam 1W — Repeater example build/config
# Copy to firmware repo or use as reference for platformio.ini build_flags
# Env: T_Beam_1W_SX1262_repeater

# Device identity (advertised name)
ADVERT_NAME=MyTBeamRepeater

# Admin (set your own in production)
ADMIN_PASSWORD=changeme

# Repeater typically relays all traffic; no room password.
# Region and PA limits (22 dBm, 800 µs ramp) are in variant/platformio — see shared/t_beam_1w/RF_PA_FAN_PMU.md
